
<div class="container mt-4">
  <div class="row">
    <div class="col-6 m-auto">
      <div class="flat-form">
      <h1>Cocktail</h1>
        <%= simple_form_for instance do |f| %>
          <%= f.input :name %>
          <%= f.button :submit, "Save Cocktail", class: "btn  btn-outline-primary"%>
          <%= link_to "Cancel", cocktails_path, class: "btn btn-outline-danger"%>
        <% end %>
      </div>
    </div>
  </div>
</div>
